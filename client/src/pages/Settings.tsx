import React from 'react';
import {Container, Row} from "react-bootstrap";

const Settings = () => {
  return (
    <Container>
      <Row>
        <h1>Settings</h1>
      </Row>
    </Container>
  );
};

export default Settings;